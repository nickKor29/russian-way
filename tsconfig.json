{
  "compilerOptions": {
    "target": "es2016" /* Устанавливает версию JavaScript, которую будет генерировать TypeScript. В данном случае это ES2016. */,
    "module": "commonjs" /* Указывает, какой формат модулей использовать при генерации кода. Здесь используется CommonJS. */,
    "jsx": "react-jsx" /* Включает поддержку JSX для React компонентов. */,
    "esModuleInterop": true /* Добавляет дополнительный код для поддержки импорта модулей CommonJS. */,
    "forceConsistentCasingInFileNames": true /* Гарантирует, что регистр символов в именах файлов совпадает при импорте. */,
    "strict": true /* Включает все строгие опции проверки типов TypeScript, такие как strictNullChecks, noImplicitAny и другие. */,
    "skipLibCheck": true /* Пропускает проверку типов внутри файлов .d.ts (типовые файлы библиотек). Это ускоряет компиляцию. */,
    "resolveJsonModule": true /* Включает поддержку импорта файлов .json. */,
    "allowJs": true /* Разрешает включать JavaScript файлы в проект TypeScript. Это полезно при поэтапной миграции проекта. */,
    "noEmit": true /* Отключает генерацию JavaScript файлов при компиляции. Полезно, если ты используешь TypeScript только для проверки типов и компилируешь с помощью другого инструмента (например, Webpack). */
  }
}
